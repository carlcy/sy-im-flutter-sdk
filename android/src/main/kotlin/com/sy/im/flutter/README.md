# Removed (0.3.0+)

`SyImFlutterSdkPlugin` MethodChannel no-op shell was removed.
Use Dart `SyIm` / `OpenIMAdapter` + dependency `flutter_openim_sdk` only.
This directory is intentionally empty of plugin code.
